# comments-service

**Запуск сервиса**

```
docker-compose up

```

Сервис будет доступен на localhost:9000

**Запуск тестов**

```
docker-compose exec app /bin/bash -c "cd e2e && ./runtests.sh"

```

